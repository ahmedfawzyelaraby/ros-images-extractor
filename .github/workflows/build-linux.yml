name: CI

on: 
  push:
    branches: 
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the dependencies Ubuntu docker image
      run: docker build . --file Dockerfile --tag rosbag-image-extractor-dependencies
